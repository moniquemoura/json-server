{
  "name": "json-server-resilia",
  "version": "1.0.0",
  "description": "Json Server",
  "main": "json-server --watch db.json",
  "scripts": {
    "start": "json-server --watch db.json"
  },
  "author": "Monique Moura",
  "license": "Private",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
